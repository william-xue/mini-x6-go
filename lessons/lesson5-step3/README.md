# Lesson 5.3 · 折点手柄

已完成并冻结。后续步骤不得修改本目录。

在冻结的 5.2 上增加：

- `Edge.vertices` 手工折点数组
- `<polyline>` 按 source → vertices → target 渲染
- 紫色折点手柄及拖动
- 边命中从单线段升级为逐段命中

页面：`http://localhost:4173/lessons/lesson5-step3/demo/`
