# Lesson 2 · 端口

已完成并冻结。后续课程不得修改本目录。

基于 Lesson 1 增加：

- `Node.ports` 归一化端口数据
- `Model.getPortPoint()` 坐标换算
- `<circle>` 端口渲染
- `{ cell, port }` 引用式端点和严格无效状态

页面：`http://localhost:4173/lessons/lesson2/demo/`
