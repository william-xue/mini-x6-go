# Lesson 7.2 · SVG 整体背景

已完成并冻结。后续步骤不得修改本目录。

- SVG 文本作为一个 Background 资源保存在 Model
- View 编码为 data URL，交给单个 `<image>` 显示
- 不用 `innerHTML`，不解析 SVG 内部图元
- 我们自己的 Primitive 继续覆盖在背景上

页面：`http://localhost:4173/lessons/lesson7-step2/demo/`
