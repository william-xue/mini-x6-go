# Lesson 6.1 · 空白拖动框选

已完成并冻结。后续步骤不得修改本目录。

- 空白 `pointerdown` 进入 selecting
- 临时 SVG `<rect>` 跟随鼠标
- 松手后用矩形相交算法查询节点
- 正向、反向拖框统一归一化为 `{x,y,width,height}`

页面：`http://localhost:4173/lessons/lesson6-step1/demo/`
