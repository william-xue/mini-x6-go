# Lesson 7.1 · 基础图元设计器

已完成并冻结。后续步骤不得修改本目录。

- 四种 Primitive 数据：rect/circle/ellipse/line
- 每个图元以 `{x,y}` 作为 `<g transform>` 原点
- 类型只决定组内局部 SVG 几何
- 四种图元共用同一套点选和拖动逻辑

页面：`http://localhost:4173/lessons/lesson7-step1/demo/`
