# Lesson 7.3 · 不规则轮廓引脚吸附

已完成并冻结。后续步骤不得修改本目录。

- 背景 SVG 仍是不可编辑整体
- `snapOutline` 是元件定义明确提供的闭合多边形
- 点击点投影到所有线段，选择距离最近的投影点
- 根据轮廓中心与线段法线确定向外方向
- 引脚同时保存局部坐标、归一化坐标和 direction

页面：`http://localhost:4173/lessons/lesson7-step3/demo/`
